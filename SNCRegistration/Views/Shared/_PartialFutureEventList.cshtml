@model IEnumerable<SNCRegistration.ViewModels.FutureEventModel>

<tr>
    <td colspan="3">
        <h4>Total: @Model.Count()</h4>
    </td>
</tr>

@foreach (var item in Model)
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VolunteerFirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VolunteerLastName)
        </td>
        <td>
            @Html.DisplayNameFor(model => model.BSDescription)
        </td>
        <td>
            @Html.DisplayNameFor(model => model.UnitChapterNumber)
        </td>
    </tr>
}
